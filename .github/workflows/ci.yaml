name: Continuous Integration

on:
  workflow_call:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v7

      - name: Build Docker Image
        run: |
          docker build \
            -t financial-api:test \
            .

      - name: Test Docker Image
        run: |
          docker images financial-api:test